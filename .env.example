# Copia estos valores como variables de entorno del servicio Compose en Dokploy
DB_PASSWORD=cambia-esta-password-de-bd
DB_ROOT_PASSWORD=cambia-esta-password-root
# Genera uno con: openssl rand -base64 64 | tr -d '\n'
APP_JWT_SECRET=cambia-esto-por-un-secreto-aleatorio-de-al-menos-64-caracteres!!!!
